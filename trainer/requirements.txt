# Reproducible SplatFuse trainer stack (Python 3.10/3.11 recommended).
#
# IMPORTANT: install the CUDA 12.4 PyTorch wheel on the NVIDIA machine first:
#   python -m pip install torch==2.4.1 torchvision==0.19.1 \
#     --index-url https://download.pytorch.org/whl/cu124
# The torch requirement below then validates the version without replacing the
# already-installed +cu124 build. On this AMD development PC, the CPU wheel is
# sufficient for --doctor, --inspect-data, --self-test, and unit tests only.

torch==2.4.1
gsplat==1.5.3
pycolmap==3.12.6
numpy==2.2.6
pillow==11.3.0
opencv-python-headless==4.12.0.88
scikit-learn==1.7.2
tqdm==4.67.1
ninja==1.13.0
packaging==25.0
